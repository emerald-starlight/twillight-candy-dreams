import type { Linter, Rule } from 'eslint'
import type { UnprefixedRuleOptions } from '@stylistic/eslint-plugin-plus/rule-options'

export type * from '@stylistic/eslint-plugin-plus/rule-options'

export type Rules = {
  [K in keyof UnprefixedRuleOptions]: Rule.RuleModule
}

declare const plugin: {
  rules: Rules
  configs: {
    'disable-legacy': Linter.FlatConfig
  }
}

export default plugin
